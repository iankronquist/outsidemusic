Quick start
===========

* Make a branch  
```shell
$ git checkout -b my-branch
```
* Make virtualenv  

```shell
$ virtualenv hacking
```

* activate virtualenv  

```shell
$ source hacking/bin/activate
```

* install deps  

```shell
$ pip install -r requirements.txt
```

* go hack  
